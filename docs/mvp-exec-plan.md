# MVP 执行计划

本文档是持续更新的执行计划。每个里程碑完成后必须更新状态、验收结果、测试结果和下一步。

## 1. 状态约定

- `TODO`：未开始。
- `IN_PROGRESS`：进行中。
- `DONE`：已完成并通过验收。
- `BLOCKED`：被未确认事项或外部条件阻塞。

## 2. 当前总状态

- 当前阶段：M1 已完成。
- 当前目标：文档和工程骨架。
- 当前业务状态：尚未实现业务闭环。
- 下一阶段：M2 档案、健康约束、目标和 AI 起草计划。

## 3. 里程碑

### M1 文档和工程骨架

状态：DONE

范围：

- 创建项目规则文档。
- 创建产品、领域、架构、AI、安全规则和执行计划文档。
- 创建后端空骨架。
- 创建前端空骨架。
- 创建 Docker Compose 配置。

不包含：

- 业务表迁移。
- 业务接口。
- AI 业务逻辑。
- 规则引擎实现。

验收条件：

- README 能说明项目目标、技术栈、启动状态和文档入口。
- AGENTS.md 精简，只包含 Codex 必须知道的规则、命令、验收规则和文档索引。
- 后端测试通过。
- 前端类型检查和构建通过。
- Docker Compose 配置可校验。

测试要求：

- `/Users/sxc/Documents/tool/apache-maven-3.9.0/bin/mvn test`
- `pnpm run typecheck`
- `pnpm run build`
- `docker compose -f deploy/docker-compose.yml config`

### M2 档案、约束、目标和 AI 起草计划

状态：TODO

范围：

- 用户档案。
- 健康约束。
- 目标管理。
- AI 起草一周计划。
- 用户确认后生成第一个计划版本。

验收条件：

- AI 起草计划不能直接生效。
- 用户确认后才能生成 `ACTIVE` 计划版本。
- 健康约束可被计划生成读取。
- 关键状态转换有审计记录。

测试要求：

- 档案和健康约束应用服务测试。
- 计划草案到生效版本的状态转换测试。
- AI 草案 JSON Schema 校验测试。
- 健康约束不可被 AI 删除的规则测试。

OPEN：

- OPEN: 第一周计划草案 Schema 最终字段。
- OPEN: 具体内置安全阈值。

### M3 今日页和每日记录

状态：TODO

范围：

- 今日页读取当前计划版本。
- 每日任务完成状态。
- 徒手训练、游泳、篮球、健身房和饮食执行记录。
- 体重、血压、睡眠、疲劳、疼痛记录。

验收条件：

- 今日页能展示当天计划任务。
- 每日记录必须引用当时的计划版本。
- 历史记录不会随计划版本变化。
- 游泳训练可记录呛水次数和换气困难评分。

测试要求：

- 每日记录创建测试。
- 计划版本引用不可变测试。
- 各训练类型最小字段校验测试。
- 前端记录页表单基础测试。

OPEN：

- OPEN: 篮球训练喘息评分枚举或评分范围。
- OPEN: 饮食执行评分范围。

### M4 周分析和安全规则

状态：TODO

范围：

- 生成 7 天周分析。
- 汇总完成率、体重、血压、睡眠、疲劳、疼痛和训练负荷。
- 实现 MVP 确定性规则。

验收条件：

- 周分析可从真实记录生成。
- 规则输出包含 `ALLOW`、`WARN`、`BLOCK`。
- 被规则阻断的调整类型明确。

测试要求：

- 周分析统计测试。
- 疼痛阻断升级测试。
- 睡眠不足阻断升级测试。
- 游泳呛水阻断升级测试。
- 完成率过低阻断增量测试。

OPEN：

- OPEN: 周分析是否允许手动重算。

### M5 AI 调整建议

状态：TODO

范围：

- OpenAI 兼容接口调用。
- 结构化 JSON 输出。
- JSON Schema 校验。
- 语义校验。
- 规则后置校验。
- 建议进入调整确认页。

验收条件：

- 无效 JSON 被拒绝。
- 引用不存在计划项的建议被拒绝。
- 被规则阻断的建议不可应用。
- 用户能看到依据、风险和前后差异。

测试要求：

- AI 客户端适配器测试。
- Schema 校验失败测试。
- 语义校验失败测试。
- 规则后置阻断测试。

OPEN：

- OPEN: 实际 AI 供应商和模型。
- OPEN: AI 调用失败降级策略。

### M6 调整确认和新计划版本

状态：TODO

范围：

- 全部接受。
- 部分接受。
- 全部拒绝。
- 接受项生成新计划版本。
- 审计记录。

验收条件：

- 只有用户确认的建议能应用。
- 新计划版本保留来源版本。
- 旧执行记录不变。
- 前后差异可查看。

测试要求：

- 全部接受状态转换测试。
- 部分接受状态转换测试。
- 拒绝状态转换测试。
- 新旧版本差异测试。
- 审计记录测试。

### M7 部署、备份和个人验收

状态：TODO

范围：

- Windows 10 + Docker 部署说明。
- Tailscale 访问说明。
- 本地数据导出或备份。
- 端到端人工验收。

验收条件：

- 台式机上可运行。
- Tailscale 可访问。
- 可导出关键健康数据。
- 完整闭环可跑通。

测试要求：

- Docker Compose 配置测试。
- 备份导出测试。
- 恢复演练测试。
- 端到端流程测试。

OPEN：

- OPEN: 备份方式和保留策略。
- OPEN: 是否需要一键启动脚本。
