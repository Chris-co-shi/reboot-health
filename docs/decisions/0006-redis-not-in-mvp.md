# 0006 Redis 暂不进入 MVP 业务流程

## 状态

已确认

## 决策

用户 PC 上有 Redis，但 MVP 暂不接入 Redis 业务流程。

## 原因

当前闭环可以用同步请求、PostgreSQL 和简单状态字段完成。过早引入 Redis 会增加部署、恢复和一致性复杂度。

## 影响

- M1-M6 不依赖 Redis。
- AI 调用状态先由数据库持久化。
- 提醒、队列或缓存如后续需要再评估。

## OPEN

- OPEN: 后续是否用 Redis 承担提醒调度、异步 AI 任务状态或缓存。
