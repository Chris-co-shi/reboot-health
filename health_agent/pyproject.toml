[project]
name = "reboot-health-health-agent"
version = "0.1.0"
description = "Python-first Health Agent Backend for reboot-health."
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
