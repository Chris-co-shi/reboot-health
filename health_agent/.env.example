# 只放 secrets。非 secret 配置后续放 agent/settings.py 或独立 config。
# 默认 smoke 使用 mock。显式使用真实模型时：
# REBOOT_HEALTH_AGENT_PROVIDER=openai-compatible

REBOOT_HEALTH_MODEL_BASE_URL=
REBOOT_HEALTH_MODEL_API_KEY=
REBOOT_HEALTH_MODEL_NAME=
REBOOT_HEALTH_MODEL_TIMEOUT_SECONDS=30

# 旧命名仍由代码兼容，但新配置请优先使用 REBOOT_HEALTH_MODEL_*。
# AGENT_OPENAI_BASE_URL=
# AGENT_OPENAI_API_KEY=
# AGENT_OPENAI_MODEL=
# AGENT_OPENAI_TIMEOUT_SECONDS=30
